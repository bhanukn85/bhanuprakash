sudo apt install git
git clone https://github.com/username/repository

To add files to git location : 

git add *
git commit -m "Comments"
git push orgin master   -- (git push origin branch_name)

git -- help
#for git help

# To remove the files
git rm file1.txt
git commit -m "remove file1.txt"

#want to remove the file only from the Git repository and not remove it from the filesystem, use: 

git rm --cached file1.txt
git commit -m "remove file1.txt"
